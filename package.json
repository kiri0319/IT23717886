{
  "name": "tamil-transliteration-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:watch": "npx playwright test --ui",
    "test:debug": "cross-env PWDEBUG=1 npx playwright test"
  },
  "devDependencies": {
    "playwright": "1.58.0",
    "@playwright/test": "1.58.0",
    "cross-env": "^7.0.3"
  }
}
